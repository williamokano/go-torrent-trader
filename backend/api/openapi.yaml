openapi: 3.0.3
info:
  title: TorrentTrader API
  version: 0.1.0
  description: TorrentTrader private tracker API
servers:
  - url: http://localhost:8080
    description: Development
paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Health check
      tags: [system]
      responses:
        '200':
          description: Server is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
